<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jawnnypoo.geotune"
    android:installLocation="internalOnly">

    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Permissions required for maps -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <!--End permissions for maps-->

    <application
        android:name="GeoTuneApp"
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >

        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="${fabric_key}"/>

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <!-- For Maps and Places API -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${google_api_key}" />

        <provider android:name=".data.GeoTuneContentProvider"
            android:exported="false"
            android:authorities="com.jawnnypoo.geotune"/>

        <activity
            android:name="com.jawnnypoo.geotune.MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".GeoMapActivity"
            android:windowSoftInputMode="stateHidden"
            android:launchMode="singleTop"
            android:theme="@style/Transparent" />

        <activity android:name=".AboutActivity" />

        <service android:name=".service.GeofenceTransitionsIntentService" android:exported="false"/>
        <service android:name=".service.GeoTuneModService" android:exported="false" />

        <receiver
            android:name=".misc.CheckRegistrationReceiver"
            android:enabled="true"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.ACTION_POWER_CONNECTED"/>
            </intent-filter>
        </receiver>

    </application>

</manifest>
